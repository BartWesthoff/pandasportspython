class CloudFile:

    def __init__(self, id, name, parents, size, mime_type, time):
        self.id = id,
        self.name = name,
        self.parents = parents,
        self.size = size,
        self.mime_type = mime_type,
        self.time = time

